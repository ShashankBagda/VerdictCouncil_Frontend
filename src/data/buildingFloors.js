/**
 * buildingFloors.js — REMOVED pixel floor data.
 * The pixel building layout has been replaced by the multi-agent workspace
 * grid in BuildingSimulation.jsx. Exports kept as empty stubs for safety.
 */

export const BUILDING_FLOORS = [];

export function deriveRoomStatus() {
  return { activeRooms: [], completedRooms: [] };
}
